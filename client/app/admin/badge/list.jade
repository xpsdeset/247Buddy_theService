.row
  .col-lg-12
    table#pickle.table.table-hover.table-striped.table-bordered(ng-show="badge_list_ctrl.badgeList.length")
      thead
        tr
          th Id
          th Ip
          th Comment
          th Time
          th Amount
          th Type
          th Status
          th Remove
      tbody
        tr(ng-repeat='badges in badge_list_ctrl.badgeList')
          td {{badges._id}}
          td {{badges.ip}}
          td {{badges.comment}}
          td {{badges.time}}
          td {{badges.amount}}
          td {{badges.type}}
          td {{badges.status}}
          td
            a(href='#a') Remove
    .alert.alert-info(ng-show="!badge_list_ctrl.badgeList.length") No Badges